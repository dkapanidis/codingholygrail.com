import { dkapanidis } from '@utils/authors/authors'
import Banner from './banner.png';

export const meta = {
  slug: 'demystifying-stateful-apps-on-kubernetes',
  title: 'Demystifying stateful apps on Kubernetes by deploying an etcd cluster',
  authors: [dkapanidis],
  date: '2021-01-14',
  topic: 'kubernetes',
  description: 'Not all apps are the same - when we deploy them to our Kubernetes cluster, we have to take that into account. One classification of apps is between Stateful & Stateless.',
}
